{
  "name": "arb-tools",
  "version": "0.1.0",
  "private": true,
  "description": "Arbitrage with CrossEx — delta-neutral funding-rate arbitrage on Gate CrossEx (web terminal + Fastify server)",
  "license": "MIT",
  "repository": "github:pendle-finance/arbitrage-with-crossex",
  "packageManager": "yarn@1.22.22",
  "engines": {
    "node": ">=22.13"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --project unit --project server",
    "test:watch": "vitest --project unit --project server",
    "test:web": "yarn --cwd web test",
    "test:live:smoke": "LIVE_TRADE_TESTS=1 SMOKE=1 vitest run --project live tests/live/engine-smoke.test.ts --reporter=verbose",
    "test:live": "LIVE_TRADE_TESTS=1 vitest run --project live --reporter=verbose",
    "live:cleanup": "tsx tests/live/cleanup.ts",
    "server": "tsx src/server/index.ts",
    "dev": "concurrently -n server,web -c blue,magenta \"tsx watch src/server/index.ts\" \"yarn --cwd web dev\"",
    "start": "yarn --cwd web build && tsx src/server/index.ts",
    "verify": "tsc --noEmit && yarn test && yarn --cwd web typecheck && yarn --cwd web test"
  },
  "dependencies": {
    "@fastify/static": "^9.1.3",
    "axios": "^1.7.7",
    "dotenv": "^16.4.5",
    "fastify": "^5.9.0",
    "gate-api": "^7.1.8",
    "undici": "^8.10.0",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "concurrently": "^10.0.3",
    "nock": "^14.0.16",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2",
    "vitest": "^4.1.9"
  }
}
